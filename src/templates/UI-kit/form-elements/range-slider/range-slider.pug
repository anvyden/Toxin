mixin rangeSlider({ sliderIdForJs, priceIdForJs })
  -
    let rangeSliderClass = 'range-slider'
    let rangeSlider = 'range-slider__slider'
    let rangeSliderSubtitleClass = 'range-slider__subtitle'

  div(class=rangeSliderClass)
    .range-slider__heading
      block
      span(class=rangeSliderSubtitleClass id=priceIdForJs)
    .rangle-slider__wrap 
      div(class=rangeSlider, id=sliderIdForJs)