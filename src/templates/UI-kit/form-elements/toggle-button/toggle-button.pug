mixin toggleButton({ text, name, isChecked })
  -
    let toggleInputClass = 'toggle-button__input'
    let toggleLabelClass = 'toggle-button__label'
    
  .toggle-button
    label(class=toggleLabelClass)
      input(class=toggleInputClass, type='checkbox', name=name, checked=isChecked)
      span.toggle-button__span=text